spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/blog_sso?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: root
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver

mybatis:
  type-aliases-package: com.popjun.sso.cas.server.domain
  mapper-locations: classpath:mapper/*.xml
